<h1>My Tasks:</h1>
<ul>
  <% @tasks.each do |task| %>
    <li> <%= link_to task.title, task_path(task) %>: <%=task.completed ? "completed 😊" : "not completed 😭" %> <%= link_to "Delete Task", task_path(task), method: :delete, data: {confirm: "Are you sure?"} %></li>
  <% end %>
  <li><%= link_to 'Create a new task', new_task_path %></li>
</ul>


